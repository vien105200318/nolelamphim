---
import { movieCardHTML } from '../lib/movieCard'
import type { Movie } from '../lib/types'

interface Props {
  movie: Movie
  dot?: 'new' | 'hot'
}

const { movie, dot } = Astro.props
---

<div set:html={movieCardHTML(movie, dot)} />
